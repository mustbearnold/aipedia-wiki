---
import ToolLogo from './ToolLogo.astro';

interface ToolRef {
  slug: string;
  title: string;
}

interface SourceRef {
  url: string;
  title?: string;
}

interface Props {
  severity?: string;
  date?: string;
  summary?: string;
  tools?: ToolRef[];
  categories?: string[];
  sources?: SourceRef[];
}

const {
  severity = 'minor',
  date = '',
  summary = '',
  tools = [],
  categories = [],
  sources = [],
} = Astro.props;

const severityCopy: Record<string, string> = {
  breaking: 'High urgency. This may change what readers should verify or compare next.',
  major: 'Material update. Likely to affect buying, ranking, workflow, or trust decisions.',
  minor: 'Context update. Useful to know, but low immediate switching pressure.',
};

function labelize(value: string): string {
  return String(value || '').replace(/^ai-/, '').replace(/-/g, ' ').replace(/\b\w/g, (c) => c.toUpperCase());
}

function host(value: string): string {
  try {
    return new URL(value).hostname.replace(/^www\./, '');
  } catch (_) {
    return '';
  }
}

const sourceHosts = Array.from(new Set(sources.map((source) => host(source.url)).filter(Boolean))).slice(0, 4);
---

<aside class="news-impact home-surface-card not-prose" aria-label="Story impact" data-reveal>
  <div class="news-impact-head">
    <span>Reader impact</span>
    <strong class={`news-impact-severity news-impact-severity-${severity}`}>{severity}</strong>
  </div>
  <p>{severityCopy[severity] ?? severityCopy.minor}</p>

  <div class="news-impact-grid">
    <div class="home-surface-inner">
      <span>Why it matters</span>
      <strong>{summary || 'This story was published because it changes the context around one or more AI tools.'}</strong>
    </div>
    <div class="home-surface-inner">
      <span>Source base</span>
      <strong>{sources.length} source{sources.length === 1 ? '' : 's'}{sourceHosts.length ? ` | ${sourceHosts.join(', ')}` : ''}</strong>
    </div>
    <div class="home-surface-inner">
      <span>Date</span>
      <strong>{date || 'Recent'}</strong>
    </div>
  </div>

  {(tools.length > 0 || categories.length > 0) && (
    <div class="news-impact-links">
      {tools.length > 0 && (
        <div class="home-surface-inner">
          <span>Affected tools</span>
          <div>
            {tools.map((tool) => (
              <a class="home-surface-pill-link" href={`/tools/${tool.slug}/`}>
                <ToolLogo slug={tool.slug} title={tool.title} size={22} />
                <span>{tool.title}</span>
              </a>
            ))}
          </div>
        </div>
      )}
      {categories.length > 0 && (
        <div class="home-surface-inner">
          <span>Categories</span>
          <div>
            {categories.map((category) => (
              <a class="home-surface-pill-link" href={`/categories/${category}/`}>{labelize(category)}</a>
            ))}
          </div>
        </div>
      )}
    </div>
  )}
</aside>

<style>
  .news-impact {
    margin: 0 0 24px;
    padding: 18px;
  }

  .news-impact-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    margin-bottom: 12px;
  }

  .news-impact-head > span {
    color: var(--home-accent);
    font-size: 11px;
    font-weight: 900;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .news-impact-severity {
    border: 1px solid rgba(249, 115, 22, 0.3);
    border-radius: 999px;
    color: var(--home-accent);
    font-size: 10px;
    font-weight: 900;
    letter-spacing: 0.12em;
    padding: 5px 9px;
    text-transform: uppercase;
  }

  .news-impact-severity-breaking,
  .news-impact-severity-major {
    background: rgba(249, 115, 22, 0.1);
  }

  .news-impact p {
    margin: 0 0 14px;
    color: var(--home-on-card);
    font-size: 14px;
    font-weight: 760;
    line-height: 1.5;
  }

  .news-impact-grid {
    display: grid;
    grid-template-columns: 1.4fr 1fr auto;
    gap: 10px;
  }

  .news-impact-grid > div,
  .news-impact-links > div.home-surface-inner {
    min-width: 0;
    padding: 11px;
  }

  .news-impact-grid span,
  .news-impact-links > div.home-surface-inner > span:first-child {
    display: block;
    margin-bottom: 5px;
    color: var(--home-on-card-muted);
    font-size: 10px;
    font-weight: 900;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .news-impact-grid strong {
    display: block;
    color: var(--home-on-card);
    font-size: 12.5px;
    font-weight: 780;
    line-height: 1.35;
  }

  .news-impact-links {
    display: grid;
    gap: 10px;
    margin-top: 10px;
  }

  .news-impact-links > div.home-surface-inner > div {
    display: flex;
    flex-wrap: wrap;
    gap: 7px;
  }

  @media (max-width: 780px) {
    .news-impact-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
