# Worker Report Template

## Changed Files

- `<tool>`: `src/content/tools/<slug>.md`

## Sources Checked

- `<tool>`: `<url>` (`primary-confirmed` or other label)

## Proposed Registry Rows

- `id`: `<source-id>`
- `label`: `<source label>`
- `url`: `<url>`
- `type`: `<official|docs|pricing|blog|newsroom|third_party|affiliate|repository|release_notes|report>`
- `trust_tier`: `<primary|secondary>`
- `volatility`: `<high|medium|low>`
- `last_checked`: `<YYYY-MM-DD>`

## Parent Hub Notes

- `<category route>`: `<what the integrator should inspect or update>`

## Verification

- `<command>`: passed/failed

## Unresolved Or Caveated Claims

- `<tool>`: `<claim>` (`primary-conflict|account-gated|checkout-gated|region-rendered|blocked-live-check|secondary-only`)
